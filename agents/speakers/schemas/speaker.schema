type Group {
    uuid: String
    name: String

    muted: Boolean
    playing: Boolean
    volume: Int
    queue: Queue

    speakers: [Speaker]
}

type Speaker {
    uuid: String
    name: String
    muted: Boolean
    playing: Boolean
    volume: Int
    queue: Queue
}
